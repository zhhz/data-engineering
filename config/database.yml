default: &default
  adapter: mysql2
  encoding: utf8
  reconnect: false
  pool: 5
  timeout: 5000
  username: root
  password:
  socket: /tmp/mysql.sock

development:
  <<: *default
  database: lscc_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: lscc_test

production:
  <<: *default
  database: lscc_production
